<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nblog.dao.UserDao">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache" 
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	<sql id="selectId">
		UserNo,
		UserName,
		(select group_concat(Name) from Role
		where Role.Id
		in (SELECT RoleId FROM UserRole WHERE
		UserNo=User.UserNo) ) RoleName,
		Password,
		Description,
		Locked,
		CredentialsSalt,
		SignTime
	</sql>

	<select id="findUserPage" resultType="com.nblog.bean.User">
		select
		<include refid="selectId" />
		from User
		where 1 = 1
		<if test="UserName != null and UserName != ''">
		and UserName like '%${UserName}%'
		</if>
		<if test="column != null">
          order by ${column} ${sort}
        </if>
	</select>
	<select id="findUserByName" resultType="com.nblog.bean.User" parameterType="com.nblog.bean.User">
		select * from User where 1=1 and UserName=${UserName}
	</select>
	
</mapper>